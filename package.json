{
  "name": "browser-in-browser",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "npm install",
    "build": "tsc",
    "start": "node dist/index.js",
    "render-build": "npm install && npx puppeteer install chrome && npm run build",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "express": "^4.21.2",
    "puppeteer": "^24.27.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/node": "^24.9.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
